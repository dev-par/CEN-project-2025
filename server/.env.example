# MongoDB connection string (replace with your actual Atlas URI)
ATLAS_URI=

# JWT Secret for signing authentication tokens (use a strong random string)
JWT_SECRET=

# Port to run the server (optional, defaults to 5050)
PORT=5050
